# Static site resources

Reserve for small files that must be copied unchanged by the application build. Production GLBs remain in the repository's canonical `assets/runtime/` tree and are delivered through the planned catalog-aware build adapter. This folder must not expose Blender sources, imported packages, or review artifacts.
