# Game session

Coordinate the fixed-step loop, command dispatch, snapshots/events, input, presentation, persistence, and progression. Simulation validates gameplay actions; the session coordinates browser services. Keep selection and camera state separate from authoritative gameplay state.
