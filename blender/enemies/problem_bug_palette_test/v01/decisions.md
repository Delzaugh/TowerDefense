# Bug palette texture experiment

- User authorized a Bug vertex-colour versus palette-texture experiment on 2026-09-13. This separately registered comparison asset leaves problem_bug v01 unchanged and is not selected by gameplay.
- Copy the authoritative Bug v01 revision 6 Blender source. Preserve the cobalt shell, red accents, removed lightning bolt, continuous leg attachments, scale, rig, anchors and move/hit/resolve clips. Keep the 2,500 triangle ceiling.
- Replace base vertex colours with a packed 32 x 4 sRGB PNG containing eight named swatches (seven currently used), assigning each face corner to its role's swatch centre. Keep one material, linear non-mipmapped sampling and existing roughness/metallicity.
- Preserve the actual baseline GLB's uniform white emission at 0.08. Its Blender vertex-emission link does not export as per-vertex emission; connecting the new atlas to emission would change the shipped appearance and confound this experiment.
- Retain semantic role IDs for source editing; remove COLOR_0 from the candidate. Existing Inspector controls support only whole-material tint on this candidate. No Inspector or shared validator changes are part of this experiment.
- The existing manifest.palette declares vertex-colour validation. The candidate instead records palette/UV expectations in experiment.json and checks them against the exported GLB in the experiment audit. All standard guarded delivery checks remain active.
- Candidate is a manual Blender source after conversion. Use ordinary guarded export for further edits; convert_palette.py refuses to overwrite the candidate or changed baseline data.
- Experiment delivered and reviewed at revision 2: 2,334 triangles, 1 mesh/material, 16 bones, 3 unchanged clips, one 32 x 4 atlas. GLB shrinks from 336,116 to 318,160 bytes. Geometry/rig/animation parity and colour/UV checks pass; visual differences remain within 2/255 per channel in matched pipeline views. The existing Inspector offers whole-material tint only. Full evidence and measured limitations are in README.md and validation/.
- User requested texture palette editing support, 2026-09-13. Added manifest texturePalettes metadata for the seven used solid swatches. The Inspector now supports independent base-colour previews, reset/reload, and exact rectangle/colour refinement notes. Shared delivery validates swatch declarations and actual image colours. This supersedes the whole-material-only editor limitation above; Blender source, packed PNG and runtime GLB bytes are unchanged.
