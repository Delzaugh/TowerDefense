# Nose, pack and reference-led hair — revision 17

Runtime SHA-256: `565351f2942e839765e1e24ddc54f134b67003db6afa913337e897d15ff0064e`

Source SHA-256: `c98aebb17904f6605c42c2d0af3bc05c74aa3165e772d78812c2a61be35deb46`

3977 / 4000 triangles; 4 meshes/materials; 0 textures; 15 bones.

The continuous nose bridge/tip is retained and nostril marks removed. The navy backpack backing box is absent, with both straps seated directly into the green tier. The TRUST word and five requested tier colors remain intact. Listen is absent from source and runtime, the contract and current review scripts; the Inspector's dynamic clip menu contains idle, work and celebrate_team.

The photographic hairstyle reference replaces the three transverse quiff locks with cropped sides, an exposed forehead, a fuller rearward sweep and an offset part. Top geometry is a shared-vertex curved surface with shallow comb channels and a seated perimeter. A personal intermediate pass rejected separate roll-like locks and a visible temple intersection. The final silhouette is reviewed in the actual Three.js GLB, not just Blender.

Personally inspected final close front, right profile, left profile, elevated oblique and top views in `nose-pack/`, together with side/rear strap and TRUST views. The top view uses a modest zoom to keep the camera outside the head. Reviewed work at 1.5 s, celebrate_team clap contact at 1.26 s, and phone-scale rest in `extra-review/`; generated remaining fixed views and clip entry/return samples as supplementary evidence. State files bind these captures to this runtime hash.

`report.json` passes with no errors or warnings. `clip-audit.json` passes all three clips, matching rest endpoints within floating-point tolerance and with zero root/base motion. The existing user Inspector visibly reports revision 17, hash prefix 565351f2942e, and 3977 / 4000 triangles.

Technical validation and personal review are complete; user artistic acceptance is separate. Hair strands are simplified into broad sculpted ridges for the game budget; the face remains static and fingers use the existing grouped rig. Prior listening evidence belongs to historical revisions and is not evidence of a current clip.
